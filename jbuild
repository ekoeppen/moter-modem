; vim:ft=lisp:ts=2:set expandtab:

(jbuild_version 1)
(executable (
             (name moter_modem)
             (public_name moter_modem)
             (package moter)
             (libraries (core
                          lwt
                          lwt.unix
                          cmdliner
                          fmt
                          fmt.cli
                          fmt.tty
                          logs
                          logs.fmt
                          logs.cli
                          tls
                          tls.lwt
                          x509
                          unix
                          cbor
                          extunix
                          mqtt_lwt
                          hex))
             (preprocess (pps (lwt_ppx ppx_let ppx_jane)))
             ))
