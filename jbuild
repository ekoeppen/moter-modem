; vim:ft=lisp:ts=2:set expandtab:

(jbuild_version 1)
(executable (
             (name moter_modem)
             (libraries (core
                          lwt
                          lwt.unix
                          cmdliner
                          fmt
                          fmt.cli
                          fmt.tty
                          logs
                          logs.fmt
                          logs.cli
                          tls
                          tls.lwt
                          x509
                          unix
                          cbor
                          mqtt_lwt))
             (preprocess (pps (lwt_ppx ppx_let ppx_jane)))
             ))
