; vim:ft=lisp:ts=2:set expandtab:

(jbuild_version 1)
(executable (
             (name modem)
             (public_name moter-modem)
             (package moter-modem)
             (libraries (core
                          lwt
                          lwt.unix
                          cmdliner
                          fmt
                          fmt.cli
                          fmt.tty
                          logs
                          logs.fmt
                          logs.cli
                          tls
                          tls.lwt
                          x509
                          unix
                          cbor
                          extunix
                          mqtt_lwt
                          hex))
             (preprocess (pps (lwt_ppx ppx_let ppx_jane)))
             ))
